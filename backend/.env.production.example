# Backend Production Environment Configuration
# Este archivo debe ser usado en el servidor de producción
# Copiar a .env y completar con valores reales

# =================================
# ENVIRONMENT
# =================================
NODE_ENV=production
PORT=3000

# =================================
# DATABASE CONFIGURATION
# =================================
# Host: localhost si PostgreSQL está en el mismo servidor
# Port: 5432 es el puerto por defecto de PostgreSQL
DB_HOST=localhost
DB_PORT=5432
DB_USERNAME=aegg_user
DB_PASSWORD=CHANGE_THIS_PASSWORD_MIN_16_CHARS
DB_NAME=aegg_db

# =================================
# JWT AUTHENTICATION
# =================================
# Generar JWT_SECRET con PowerShell:
# -join ((65..90) + (97..122) + (48..57) | Get-Random -Count 32 | % {[char]$_})
JWT_SECRET=CHANGE_THIS_SECRET_KEY_MIN_32_CHARS_RANDOM
JWT_EXPIRATION=7d

# =================================
# CORS CONFIGURATION
# =================================
# Dominio del frontend (sin trailing slash)
CORS_ORIGIN=https://aegg.creapolis.mx

# =================================
# OPTIONAL - FILE UPLOADS
# =================================
# Si tienes límites específicos de archivos
# MAX_FILE_SIZE=10485760  # 10MB en bytes
# UPLOAD_PATH=/var/www/vhosts/creapolis.mx/aegg-api/uploads

# =================================
# OPTIONAL - EMAIL (SI LO IMPLEMENTAS)
# =================================
# SMTP_HOST=smtp.gmail.com
# SMTP_PORT=587
# SMTP_USER=tu_email@dominio.com
# SMTP_PASS=tu_password_de_app
# EMAIL_FROM=noreply@aegg.creapolis.mx

# =================================
# OPTIONAL - LOGGING
# =================================
# LOG_LEVEL=info  # error, warn, info, debug
# LOG_FILE=/var/www/vhosts/creapolis.mx/aegg-api/logs/app.log

# =================================
# NOTES
# =================================
# - NUNCA commitear este archivo a Git
# - Usar valores únicos para producción
# - Guardar backup seguro de estas credenciales
# - Actualizar passwords periódicamente
